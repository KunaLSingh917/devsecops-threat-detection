apiVersion: v1
kind: Service
metadata:
  name: devsecops-service
  namespace: devsecops

spec:
  selector:
    app: devsecops-app

  type: NodePort

  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30080
